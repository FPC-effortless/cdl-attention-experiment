# Unified harness v2 replay trigger

This marker intentionally changes no model, benchmark, or training code. It exists only to trigger `.github/workflows/replay-casm-unified-v2.yml` on the `benchmark/unified-harness-v2` branch so the historical CASM intervention families are retrained and evaluated through the same unified v2 harness revision.

Triggered: 2026-08-31
